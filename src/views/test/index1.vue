<template>
  <div />
</template>

<script>
export default {
  data() {
    return {
      /*      dicts: {
        dictArr: [
          {}
        ]
      }*/
    }
  },
/*  install(Vue) {
    var dictArr = this.dicts.dictArr // 所有数据字典
    for (var key in dictArr) {
      if (dictArr.hasOwnProperty(key)) { // key是字典唯一ID
        (function(key) {
          Vue.filter(key, function(n) { // n?
            var item = dicts.getDict(key) // 根据key获取数据字典
            return item[n + ''] || '-'
          })
        }(key))
      }
    }
  }*/
}
</script>

<style scoped>

</style>
